query user1 {
    user(by: {id:1}) {
        slug
        name
    }
}

query userA {
    user(by: {email: "alice@nowhere.com"}) {
        slug
        name
    }
}

query two {
    tom: user(by: { id: 1 }) {
        slug
    }

    jerry: user(by: { email: "jerry@warnerbros.com" }) {
        slug
    }
}

query invalid {
    user(by: { id: 1, email: "jerry@warnerbros.com" }) {
        name
    }
}
